/**
 * JPE Studio Design System — Barrel Export
 * Cyberpunk theme for IDE interface
 */

export { tokens, type DesignTokens } from './tokens';
export { default } from './tokens';

// Figma-compatible alias
export { tokens as T } from './tokens';
