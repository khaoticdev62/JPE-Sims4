/**
 * Transformation Service - Legacy Compatibility Re-export
 *
 * This module re-exports the canonical TransformationService from
 * TransformationService.ts for backward compatibility. All new code
 * should import from '@/services/TransformationService' directly.
 *
 * @deprecated Use '@/services/TransformationService' instead.
 */

export { TransformationService } from './TransformationService'
