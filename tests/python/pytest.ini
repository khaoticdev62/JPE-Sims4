[pytest]
testpaths = tests/python
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --junitxml=junit-report.xml
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
